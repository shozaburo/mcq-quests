# 🗂 MCQサイト（新・本番用）構造ガイド

浅く・コンパクト・全部相対パス。**このフォルダの中身をそのまま本サイトにアップすれば動きます**（re-gi.jp でも mandalachart.jp でも、どの階層でもOK）。

```
mcq-site/
  index.html              ← トップ（タイトル→ワールド選択）
  card.html               ← 挑戦者カード（全ステージ共通）
  nyumon.html             ← パソコン初心者教室（帝国なし・大塚麗夏）
  assets/                 ← 全ステージ共通の中身
    engine.js  bgm.js  style.css  config.js  nyumon-lines.js
    bgm/     A.mp3〜H.mp3, tutorial.mp3
    stages/  A.png〜H.png（ステージ背景）
    venue-kominka.png
  stages/
    google64/             ← ステージ①（β）
      index.html          ボード（9×9）
      quest.html          全クエスト共通（quest.html?id=A1 で切替）★64→1
      cutscene.html       カットシーン（?area=A&scene=op）
      data/               config / characters / areas / progress / urls / scenes
                          quests/A〜H.js（クイズ）/ missions/A〜H.js（実践）
    claude64/             ← ステージ②（γ）※同じ構成
    _template/            ← 新ステージ追加用のひな形
```

## 🎨 テーマは data/config.js で自動切替
- `board.color`（例 #FFD700 金／#7e57c2 紫）でボード全体の配色
- `goalId` / `goalName` で見出し・タイトル
- 背景画像 `board.imageUrl`、閾値 `board.threshold`

## ➕ 新しいステージを増やすには
1. `stages/_template` フォルダを丸ごとコピーして `stages/新ステージ名/` にする
2. その中の `data/` に新ステージのデータ一式を入れる（config・characters・areas・progress・urls・scenes・quests・missions）
3. トップ `index.html` の「ワールド選択」に、新ステージへのカードを1枚追加
   （`<a class="world" href="stages/新ステージ名/index.html">…`）

これだけ。共通部分（engine/bgm/style/card/nyumon）はいじらなくてOK。

## 🎵🖼 素材（背景・BGM）は「各ステージのフォルダ」に置く
ステージごとに別の背景・音楽にできるよう、素材は**各ステージ内**に入れます（共有ではない）。

- 背景画像 → `stages/{ステージ}/bg/A.png 〜 H.png`
- BGM      → `stages/{ステージ}/bgm/A.mp3 〜 H.mp3`

例）
- β（google64）：`stages/google64/bg/`・`stages/google64/bgm/`（配置済み）
- γ（claude64）：`stages/claude64/bg/`・`stages/claude64/bgm/`（ここにγ素材を入れる）

※ 初心者教室のBGMだけは共通で `assets/bgm/tutorial.mp3`。

## 📌 メモ
- 旧GitHub版（shozaburo.github.io）は残してあります。これはそれとは別の「本番用」一式です。
- 素材（BGM/背景）は `assets/bgm/` `assets/stages/` に置きます。
- GAS連携（EXP・お土産・サンクス）を使うときは、デプロイ後のURLを
  `assets/config.js` と各 `stages/*/data/config.js` の questApiUrl に貼ります。
